#ifndef CURRENCY_CONVERTER_H
#define CURRENCY_CONVERTER_H

class CurrencyConverter {
public:
    static float convertToEUR(float amount);
    static float convertToGBP(float amount);
};

#endif
